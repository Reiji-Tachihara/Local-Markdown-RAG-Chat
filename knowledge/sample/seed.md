# Sample knowledge

This is a public sample Markdown file for Local Markdown RAG Chat.

The application reads Markdown files under `knowledge/`, splits them into chunks, creates embeddings, and uses relevant chunks as context for chat responses.

Private notes, conversation logs, and personal knowledge should not be committed to Git. Put public examples under `knowledge/sample/`.
