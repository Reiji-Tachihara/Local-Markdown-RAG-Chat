import type { PersonaKey } from "./types";

export const CHAT_INPUT_MAX_LENGTH = 12000;

export const personaLabels: Record<PersonaKey, string> = {
  user_clone: "ユーザー",
  rational_advisor: "相談相手"
};
